# src/main/resources/application.properties
spring.application.name=backend
server.port=8080

# PostgreSQL Azure Database
spring.datasource.url=jdbc:postgresql://sdp25db.postgres.database.azure.com:5432/postgres
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username= ${PGUSERNAME}
spring.datasource.password= ${PGPASSWORD}

# ULTRA-CONSERVATIVE connection pooling for Burstable tier
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=120000
spring.datasource.hikari.idle-timeout=60000
spring.datasource.hikari.max-lifetime=300000
spring.datasource.hikari.leak-detection-threshold=30000

# JPA Configuration for PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# CRITICAL: Reduce database overhead
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Your API Key
app.api-key=${API_KEY_ADMIN}

# Additional safety
spring.jpa.properties.hibernate.connection.provider_disables_autocommit=true
logging.level.com.zaxxer.hikari=WARN

# H2 Console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Supabase Auth configuration (for authentication only)
supabase.url=https://cixdigfxjvranfleyamm.supabase.co
supabase.key=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImNpeGRpZ2Z4anZyYW5mbGV5YW1tIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTUyMDkyNzAsImV4cCI6MjA3MDc4NTI3MH0.ZgbRo8kxPZzhJe0BEw56seYrUlf3UiylCkeRPzdGWEQ
app.supabase-jwt-secret=${SUPABASE_JWT_SECRET}
app.supabase-url=${SUPABASE_URL}

# Azure Blob configuration (for images)
# The full connection string you copied from the Azure Portal's "Access keys" page
azure.storage.blob.connection-string=${AZURECONNECTIONSTRING}

# The name of the container you created inside the storage account
azure.storage.container.name=${AZURESTORAGENAME}
azure.storage.container.name.documents=${AZURESTORAGENAMEDOCS}